services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: presenca
      POSTGRES_USER: presenca
      POSTGRES_PASSWORD: presenca
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
