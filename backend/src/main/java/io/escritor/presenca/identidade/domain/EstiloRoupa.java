package io.escritor.presenca.identidade.domain;

/** Personalização de avatar (ver {@link EstiloCabelo}) - estilo da roupa de cima (top). Bottom/
 * calçado ficam fora de propósito nesta primeira versão: o avatar é pequeno demais (visto de cima,
 * ~30px de altura) pra esses detalhes valerem o esforço de desenho separado. */
public enum EstiloRoupa {
    CAMISETA,
    MOLETOM,
    JAQUETA,
    REGATA
}
