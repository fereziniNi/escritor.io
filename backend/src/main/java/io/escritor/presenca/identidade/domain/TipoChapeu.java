package io.escritor.presenca.identidade.domain;

/** Personalização de avatar - categoria "Hat" do editor do Gather usado como referência de
 * estrutura/quantidade. `NENHUM` não desenha nada. Ganhou cor própria nesta expansão
 * (`corChapeu`) - antes a cor era fixa por estilo. */
public enum TipoChapeu {
    NENHUM,
    BONE,
    BONE_LATERAL,
    GORRO,
    CHAPEU_PRAIA,
    BANDANA,
    FAIXA,
    CARTOLA,
    CAPACETE,
    TIARA
}
