package io.escritor.presenca.identidade.domain;

/** Personalização de avatar - categoria "Facial hair" do editor do Gather usado como referência de
 * estrutura/quantidade. Usa `corCabelo` (sem paleta própria). `NENHUM` não desenha nada. */
public enum TipoBarba {
    NENHUM,
    BIGODE_FINO,
    BIGODE_GROSSO,
    CAVANHAQUE,
    SUICAS,
    BARBA_CURTA,
    BARBA_CHEIA,
    CAVANHAQUE_BIGODE
}
