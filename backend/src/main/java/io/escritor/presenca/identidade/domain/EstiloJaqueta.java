package io.escritor.presenca.identidade.domain;

/** Personalização de avatar - categoria "Jacket" do editor do Gather usado como referência de
 * estrutura/quantidade. Camada opcional por cima de {@link EstiloTop} - `NENHUMA` não desenha
 * nada, deixando o top à mostra. */
public enum EstiloJaqueta {
    NENHUMA,
    JEANS,
    BLAZER,
    MOLETOM_CAPUZ,
    COLETE,
    CASACO_LONGO,
    BOMBER,
    CARDIGA,
    COURO
}
